# ============================================================================
# 公共环境变量（**所有模式都会加载**：dev / build / preview）
# ----------------------------------------------------------------------------
# Vite 的加载顺序（后面的覆盖前面的）：
#   .env  →  .env.local  →  .env.[mode]  →  .env.[mode].local
#
# ⚠️ 把"本机后端地址"放在这里的**原因**（本机实测踩过的坑）：
#    `uni build` 内部按 **production 模式** 运行，只读 `.env` 与 `.env.production`，
#    **不会读 `.env.development`**。
#    我最初把 VITE_DEV_SERVER_ORIGIN 写在 `.env.development` 里，
#    结果小程序构建产物把它编译成了空字符串 `"".trim()`，
#    应用一启动就抛"缺少后端地址配置" —— 构建却完全成功，属于典型的"绿构建、白屏应用"。
# ============================================================================

# ---------------------------------------------------------------------------
# 本机后端绝对地址
# ---------------------------------------------------------------------------
# 谁用：
#   · 小程序端 / App 端 —— **必须绝对地址**（uni.request 不支持相对路径）
#   · Vite 代理的转发目标（见 vite.config.ts）
#
# 为什么必须有它：小程序端的 `uni.request` 遇到 `/api/...` 这样的相对路径时，
# 会把它解析到"项目域名"而不是本机后端，请求必然失败。
# 本机实测事故：小程序端因此连不上后端 → register-mode 请求失败 →
# 界面显示"当前暂未开放注册"、注册 Tab 消失（因为当时还做了静默兜底）。
#
# 开发期用 http://127.0.0.1 的前提：
#   微信开发者工具里开启「不校验合法域名」（manifest.json 已配 urlCheck:false）。
#
# ⚠️ 上线时**必须改成已备案的 https 域名**（在 .env.production 里覆盖本值），
#    否则打包出去的小程序/APP 连的是本机，等于不可用。
VITE_DEV_SERVER_ORIGIN=http://127.0.0.1:8080
