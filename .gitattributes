# =============================================================
# .gitattributes —— 统一行尾与文件类型判定
# 背景：Windows 上 core.autocrlf 默认为 true，会让 LF 与 CRLF 互相转换，
#       导致每次 diff 出现整文件级别的"假改动"。这里显式固定为 LF。
# =============================================================

# 默认：文本按 LF 入库与检出
* text=auto eol=lf

# Windows 批处理必须 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 说明：`.ps1` 保持 LF —— PowerShell 5.1 与 pwsh 7 都能正确处理。
# 但 **BOM 必须保留**（见 docs/agents/README.md §7.1）：
# git 不会因为行尾转换而丢失 BOM，但用编辑工具改写 .ps1 会丢，改完要补回来。

# 二进制：不做行尾转换，也不参与 diff
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary
*.ico  binary
*.jar  binary
*.war  binary
*.zip  binary
*.apk  binary
*.xlsx binary
*.pdf  binary

# 让 marker 文件不参与语言统计
docs/**/*.sql linguist-documentation
docs/**/*.md  linguist-documentation
