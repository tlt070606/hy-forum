# 工作计划（多 Agent 协作看板）

> **本文件回答一个问题：现在谁在动哪些文件、谁想改契约、有什么新决定要广播。**
> 使用规则见 [`README.md`](README.md)（AI 协作规范）。

---

## 0. 红线与写权

**红线：本文件只登记「协作状态」，绝不复制契约内容。**
不得把表结构、接口字段、参数数值抄进来 —— 一旦抄进来，它就成了第 3 份事实来源，必然漂移（本项目已在 `技术方案.md` §13–§15 吃过一次）。
契约永远只在 `docs/db/schema.sql` 与 `openapi.json` 里，本文件只写**指针**。

| 区段 | 写权 | 读权 |
|---|---|---|
| §1 当前状态 | **仅 L1** | 所有人 |
| §2 文件所有权 | **仅 L1** | 所有人 |
| §3 变更请求队列 | **仅 L1**（L2/L3 通过汇报提交 CR） | 所有人 |
| §4 广播区 | **仅 L1** | **所有 Agent 每次开工必读** |
| §5 Agent 登记 | **仅 L1** | 所有人 |

**为什么 L2/L3 不直接写本文件**：多个 Agent 同时写同一文件会互相覆盖（文件级写入）。让 L1 做唯一写者，冲突就从根本上消失了 —— 这也是 L1「集成者」角色的实质内容。

> L2/L3 的产出通过**任务汇报**回到 L1，由 L1 登记进本文件。
> 若将来出现必须由 L2 直写的场景，改为 `docs/agents/log/<agent-id>.md`（一 Agent 一文件），本文件只放链接。

---

## 1. 当前状态

| 项 | 值 |
|---|---|
| 当前 Wave | **W1 完成**（M1 与测试基建均交付并通过 L1 独立复核）。**W2 待规划** —— 但**前端选型已反转（uni-app）**，M2 任务书需据此重写 |
| 在跑 Agent | 无（M1 `20d4d1ce…ab130` 与测试基建 `33c43114…5a993` 均已交付） |
| 当前契约版本 | `schema.sql`：**16 张表 + 1 个 CHECK 约束，已在 MySQL 8.0.34 真机执行通过**／`openapi.json`：**不存在**（⚠️ M1 的导出链路有 CR-002 风险，待其收工核实） |
| 版本控制 | ✅ **git 仓库 + GitHub 远端均已就位**：`main` 分支，远端 `git@github.com:tlt070606/hy-forum.git`（**Private**，**SSH 认证**，无需 PAT）。3 个提交、112 文件已推送，`git status` 可用 |
| 阻塞项 | **无硬阻塞**。跟踪项：① `openapi.json` 已产出（8 路径，SHA256 `F641BBF5…`），但**尚未由 L1 冻结广播** ② CI 已推远端、**尚未确认首次运行结果**（需需求方在浏览器查看）③ **M2 任务书需按 uni-app 重写**（前端选型已反转） |
| 目标定位 | **本地可用版**（朋友/公网以后再说）→ M8 暂不启动 |
| 环境事实 | 见 [`README.md`](README.md) **§7.2** 与 [`../ops/deployment.md`](../ops/deployment.md) **§1.1／§3.1**：PowerShell 5.1（脚本须带 BOM，**改完要补 BOM**）、**JDK 21 已装好**（`D:\develop\jdk21`；**但本会话每条命令仍须显式 `$env:JAVA_HOME`**，因为宿主进程继承旧环境）、Java 控制台中文乱码需 `'-Dstdout.encoding=UTF-8'`、MySQL 8.0.34 可用（`hy_forum`＋`hy_forum_test`）、**Windows 无 Docker 且决策不装**、Redis 在 VM（`192.168.100.128:6380`）、**文件系统与网络权限已放宽但策略会变（用前先探测）**、**VM 已腾出（可用内存约 1.6 GB，磁盘仅剩 ~3 GB）** |

---

## 2. 文件所有权表

规则：**同一时刻一个路径只能有一个所有者**；所有权在 §5 登记，任务结束即交还。
包路径口径见 [`../技术方案.md`](../技术方案.md) §3.3 的 **v1.8 补充**（基础包 `com.hyforum`，七个业务包互不依赖 —— 即铁律 3 的落地方式）。

| 路径 | 所有者（当前） | 说明 |
|---|---|---|
| `docs/db/schema.sql`、`docs/db/migrations/**` | **L1 独占** | 契约；改它必须走 [`../db/README.md`](../db/README.md) 冻结流程 |
| `openapi.json` | **L1 独占** | 由后端注解导出、由 L1 冻结；**任何人不得手工编辑** |
| `server/pom.xml` | **W1-M1 任务独占** | 含完整测试依赖 + `maven-enforcer` 的 `requireJavaVersion [21,)`；**测试基建任务不得改它** |
| `server/src/main/java/com/hyforum/common/**`、`domain/**` | **W1-M1 任务** | 基础设施与实体（跨模块共享，改动影响面最大） |
| `server/src/main/java/com/hyforum/auth/**` | **W1-M1 任务** | 认证、注册模式、验证码 |
| `server/src/main/java/com/hyforum/post/**` | 空闲 | 版块、帖子、网盘字段归一化 |
| `server/src/main/java/com/hyforum/media/**` | 空闲 | OSS 直传签名与回调 |
| `server/src/main/java/com/hyforum/interaction/**` | 空闲 | 评论、点赞、收藏、关注（幂等与楼中楼不变量） |
| `server/src/main/java/com/hyforum/audit/**` | 空闲 | 敏感词、审核队列、举报、`ContentAuditService` |
| `server/src/main/java/com/hyforum/notify/**` | 空闲 | 消息通知 |
| `server/src/main/java/com/hyforum/admin/**` | 空闲 | 管理后台接口、留痕写入 |
| `server/src/test/resources/application-test.yml`、`server/src/test/java/com/hyforum/auth/**` | **W1-M1 任务独占** | 测试配置（连 `hy_forum_test` + VM Redis）与 M1 自己的测试 |
| `server/src/test/java/com/hyforum/support/**`、`.../schema/**` | **W1 测试基建任务独占** | 共享测试基建与 schema 不变量测试 |
| `web/**` | 空闲 | 前台 SPA（Vue3 + Vant） |
| `web/src/api/generated/**` | **L1 独占** | **由 `openapi.json` 生成的类型/客户端 —— 禁止手改** |
| `admin/**` | 空闲 | 管理后台独立前端工程 |
| `android/**` | 空闲 | Kotlin WebView 壳 |
| `.github/workflows/**` | **W1 测试基建任务**（产出需 L1 复核） | CI |
| `scripts/**` | **W1 测试基建任务**（新增脚本） | 校验脚本（**`.ps1` 必须带 UTF-8 BOM**，见 [`README.md`](README.md) §7.1） |
| `tests/golden-path/**` | **L1 独占** | 黄金路径 E2E 脚本 |
| `docs/**`（除本目录） | **L1 独占** | 文档一律由 L1 改，避免漂移 |

> 「L1 独占」不等于 L1 要写实现：L1 只做契约、集成、验收与文档裁决。

---

## 3. 变更请求队列（CR）

**什么时候用**：L2/L3 发现契约（表结构 / 接口字段 / 错误码）不足以完成任务时。

**流程（不要跳步）**：

```
L2 发现契约不足
  └─ 不改契约、不绕过 → 结束本轮并报告 "BLOCKED + CR 内容"
      └─ L1 裁决（批准 / 拒绝 / 改方案）
          └─ 批准：L1 改契约 → 本区登记 → §4 广播 → 用 send_message 让原 L2 继续
              └─ 其他在跑的 L2：下一轮交互时由 L1 告知，或自行读 §4 发现
```

**禁止**：L2 自己加字段、自己改 `openapi.json`、用"我这边先这么写，回头对齐"的方式绕过。

### 队列

| CR | 提出者 | 请求内容 | 影响范围 | 状态 | 裁决 / 结论 |
|---|---|---|---|---|---|
| CR-001 | 测试基建 L2 | 映射表未登记基建测试方法名 → 覆盖率脚本每次报"多余" | `docs/testing/验收项-测试映射.md` | **已实施** | 新增「测试基建」一节登记 6 条；顺带修掉脚本的**幽灵期望**缺陷（详见 §5.2 L1 复核行） |
| CR-002 | 测试基建 L2（知会） | M1 的 `springdoc-openapi-maven-plugin` 绑在 `integration-test`，而命令是 `mvn package` → **插件永不执行**，且未配 `spring-boot:start/stop` | `server/pom.xml`、契约导出链路 | **已批准并绕过** | L1 提供 [`scripts/export_openapi.ps1`](../../scripts/export_openapi.ps1)（打包→起 jar→curl→停，含依赖探活与路径校验）；M1 实测用它产出 8 路径；**M1 已删除自建的 `server/export_openapi.ps1`，工具收敛为一份** |
| CR-003 | M1 L2 | `ErrorCode` 需要契约外的兜底码 `500 服务器内部错误`（否则未捕获异常漏出 Spring 默认错误体，破坏统一响应约定） | `docs/技术方案.md` §6.1 | **已批准并登记** | `500` 已写入 §6.1 错误码表，并附两条约束：**响应体只给通用文案、绝不含堆栈/SQL/类名**；**能归到具体业务码的错误一律用具体码**，兜底不是图省事的借口 |
| CR-004 | M1 L2（归属登记） | ① `com.hyforum.admin` 被 M1 最小使用 ② `domain/**` 与 `UserVO` 归属 ③ `/api/user/me` 放在 `auth` 包 | §2 所有权表 | **已裁定** | ① M1 阶段的 `admin` 包只含**管理员登录**这一最小能力（`admin` 表与登录属 M1 范围），其余后台接口归 M6；② **`domain/**` 是共享层：任何模块可读，但改动需 L1 裁决**（它影响所有模块）；③ **保持现状** —— `/api/user/me` 是"token → 我是谁"，本质属认证域；M4 做个人主页时**新建 `com.hyforum.user`** 放 `/api/users/{id}` 等面向他人的接口 |

> 状态取值：`待裁决` / `已批准` / `已拒绝` / `已实施` / `已撤回`。

---

## 4. 广播区（L1 单向发布，**所有 Agent 每次开工必读**）

> 只写「决定」与「契约版本变化」，不写需求转述。需求一律指向 `docs/` 原文。

| 时间 | 广播 | 影响谁 |
|---|---|---|
| 2026-09-14 | **P1-3 定案**：楼中楼采用**归并语义** —— `parent_id` 恒等于 `root_id`（其所属主楼 id）。schema 已加 `chk_comment_two_levels`。写评论代码时必须遵守 §6.6 的**唯一写入口**要求 | 全部后端 |
| 2026-09-14 | **P1-4 定案**：邀请码并发必须用**条件更新 + 影响行数校验**，禁止"先 SELECT 再 UPDATE"。原文档「依赖 `uk_code` 防并发」的说法**是错的** | 全部后端 |
| 2026-09-14 | **脚本编码红线**：`.ps1` 必须带 UTF-8 BOM（否则 5.1 + GBK 下直接跑不起来）；`.sql`/`.md` 不要带 BOM | 全部 |
| 2026-09-14 | **演练环境已登记**：VMware 上的 Ubuntu 24.04（`ssh roott@192.168.100.128`，2 vCPU / 1967 MB，Docker 29.1.3 + JDK 21 已装）。**它当前被另一项目的容器栈占用约 1.6 GB、磁盘仅剩 ~3 GB、3306 被占** → 处置前不得在其中启动本项目栈。详见 [`../ops/deployment.md`](../ops/deployment.md) §1.1 | 全部 |
| 2026-09-14 | **Agent 能力边界**：本 Agent 的 shell **无直接外网**（不能下载安装包），且**只能写项目目录**（`D:\develop` 不可写）→ 系统级安装必须由需求方执行，或经 VM 中转下载后 `scp` 回主机 | 全部 |
| 2026-09-14 | **D6 环境决策**：① VM 内另一项目的容器栈**已 stop（未删除，可 `docker start` 恢复）**，腾出约 1.2 GB；② **不装 Docker Desktop**，Redis 改用 VM 内容器 `hy-redis` → **`192.168.100.128:6380`**；③ **放弃 Testcontainers**，集成测试连本机 `hy_forum_test`（`scripts/init_test_db.ps1` 每次重建）。取舍与补偿见 [`../testing/README.md`](../testing/README.md) §3.1 | 全部 |
| 2026-09-14 | ⚠️ **D6 的副作用已发现并修复，后续动作必须遵守**：D6 把 VM 里六个容器一起停掉，**连带弄断了本机另一个项目 `D:\test\hm-dianping`**（它的 Redis 指向 `192.168.100.128:6379`）。该故障**静默**：进程不报错，只是卡在 `SYN_SENT` 重连，功能全废。已把 `redis` 启回并确认 `Established`。**规矩：停任何别人可能在用的服务前，先查清谁在依赖它**（`Get-NetTCPConnection` 看端口占用方，或搜本机其它工程的配置）。**`redis`(6379) 今后必须保持运行**，腾空间只停 `rabbitmq / seata-server / nacos / nginx / mysql`。详见 [`../ops/deployment.md`](../ops/deployment.md) §1.1 | 全部 |
| 2026-09-14 | **能力边界已放宽（策略会变，用前先探测）**：文件系统 = **danger-full-access**（`D:\develop` 可写）；网络 = **已开通**。此前为"仅项目目录可写 + 无外网"。**被拒时不要硬绕，报告并请需求方放开** | 全部 |
| 2026-09-14 | **JDK 21 已就绪，W1 可派发**：Temurin 21.0.12.1 在 `D:\develop\jdk21`，用户级 `JAVA_HOME`／`PATH` 已指向它。**两个必须遵守的点**：① **本会话每条命令前设 `$env:JAVA_HOME='D:\develop\jdk21'`**（宿主进程仍持旧环境，子进程会继承 jdk17 → `<release>21</release>` 会报 `invalid target release`）；② **别依赖控制台中文输出**，需要时用 `'-Dstdout.encoding=UTF-8'`（参数整体加引号）。预检：`scripts/toolchain-check/` | 全部 |
| 2026-09-14 | **git 仓库 + GitHub 远端就位**：远端 `git@github.com:tlt070606/hy-forum.git`（Private）。**认证走 SSH，不需要 PAT**（账号密码已被 GitHub 弃用，实测 401）。`main` 已推送 112 文件。**`git status` 从此是校验 Agent 是否越界写文件的权威手段** |
| 2026-09-14 | **CI 可以真跑了**：推送会触发 `.github/workflows/ci.yml` 首次运行。**预期第一次是红的**（M1 的测试错误尚未修完）—— 红的首次恰好证明流水线真的接上了。运行结果只能由需求方在浏览器查看（Private 仓库需要认证，L1 无 PAT 无法查询 API） |
| 2026-09-14 | **`openapi.json` 已产出（M1 版）**：**8 个路径**，SHA256 `F641BBF59EF44B3D22D1A5D55512DDC1308F4F48E7319BC5D36D660ABBE15370`。**W2 起可基于它开工**；改契约必须走 §3 的 CR 流程，**任何人不得手工编辑该文件** | 全部 |
| 2026-09-14 | **前端选型反转：改用 uni-app**。交付形态扩为三端（H5 + 安卓 App + 微信小程序·**仅练习不上线**）。见 [`adr/0011`](../adr/0011-前端改用uni-app作为唯一前端.md)；[`adr/0002`](../adr/0002-前端采用Vue3与Vite与Vant.md) 已转「已被取代」。**后端零改动**。影响：**M2 任务书须按 uni-app 重写**（Vant → wot-design-uni、Vue Router → pages.json、Axios → uni.request 封装）；**M7 由 Kotlin WebView 壳改为三端打包** | 全部，尤其前端 |
| 2026-09-14 | **两条实测坑（写测试/改代码前必读）**：① **Surefire 默认不跑 `*IT.java`** → 测试类一律以 `Test` 结尾，否则 `mvn test` 会**静默跳过**它，全绿但从未执行；② **`@Valid` 校验发生在 Controller 方法体之前** → 测限流/越权/业务分支必须用**合法请求体**，否则永远先命中 400，把真实 bug 掩盖掉。详见 [`../testing/README.md`](../testing/README.md) §5.1 | 全部 |
| 2026-09-14 | 部署类决策已定：**单一配置、无档位**（[`adr/0010`](../adr/0010-部署以本地开发为基准并取消档位.md)）。禁止在代码中引入任何环境档位分支 | 全部 |
| 2026-09-14 | 目标定位为**本地可用版**：**M8 暂不启动**；但铁律 5（密钥不落前端）与铁律 8（OSS 直传）**不得因此跳过** | 全部 |

---

## 5. Agent 登记（历史记录，L1 登记）

### 5.1 进行中

| Agent | 角色 | 范围 | 开始 | 任务书 |
|---|---|---|---|---|
| W1-M1 | L2 实现者 | `server/pom.xml`、`server/src/main/**`、`server/src/test/resources/application-test.yml`、`server/src/test/java/com/hyforum/auth/**` | 2026-09-14 | [`tasks/W1-M1-后端骨架与认证.md`](tasks/W1-M1-后端骨架与认证.md) |
| W1-测试基建 | L2 实现者 | `server/src/test/java/com/hyforum/support/**`、`.../schema/**`、`.github/workflows/**`、`scripts/**` | 2026-09-14 | [`tasks/W1-测试基建.md`](tasks/W1-测试基建.md) |

> **两者并行，写权已互斥切分**（见 §2）。**已知的时序依赖**：测试基建的"跑通测试"一步依赖 M1 先建出 `server/pom.xml`；此前它只能完成不依赖 Maven 工程的部分（CI 定义、覆盖缺口脚本），**不得自行创建 pom.xml**。

### 5.2 已完成

| Agent | 角色 | 范围 | 改动文件 | 验收输出 | 未完成 / 未验证 |
|---|---|---|---|---|---|
| L1（本会话） | 主 Agent / 契约守护 + 集成 | **W0 契约冻结**：P1-3（楼中楼归并语义 + CHECK 约束）、P1-4（邀请码条件更新） | `docs/db/schema.sql`、`docs/db/migrations/20260914_add_comment_two_level_check.sql`、`docs/db/seed.sql`、`scripts/verify_m0.ps1`、`docs/技术方案.md`、`docs/adr/0005`、`docs/adr/0009`、`docs/db/README.md`、`docs/PLAN.md`、`docs/testing/验收项-测试映射.md`、`docs/review/…方案评审记录.md`、`docs/agents/**` | ① 技术方案 §5.3 与 `schema.sql` 逐行一致 PASS；② 全库 145 条相对链接可达 PASS；③ 编码约定核查 PASS；④ `verify_m0.ps1` 冒烟输出（失败路径正常、退出码 1、中文无乱码） | **M0 尚未真机执行**（缺 MySQL root 密码）；`openapi.json` 未产出；`seed.sql` 的管理员密码为占位符、敏感词库为占位数据 |
| L1（本会话） | 主 Agent / 集成 + 验收 | **M0 真机执行与验收** | `docs/db/seed.sql`、`scripts/verify_m0.ps1`（升级为**双向** CHECK 验证）、`docs/agents/README.md`（新增 BOM 编辑陷阱） | ① `verify_m0.ps1 -Init`：**20 项全 PASS，退出码 0**；② **独立复核**（不依赖该脚本自身）：逐一核对 16 张表名、直读 `information_schema.CHECK_CONSTRAINTS` 原始定义、合法 `(0,0)`/`(5,5)` 被接受、3 种非法组合被拒、清理后 `comment` 行数 = 0 | 2 条 WARN：管理员密码哈希仍是占位符（需 M1 后生成）、敏感词库仍是占位数据（需需求方选定真实词库） |
| L1（本会话） | 主 Agent / 集成 + 验收 | **D6 环境落地**：VM 内停用他项目容器栈、启用本项目 Redis、测试库基建、文档同步 | `scripts/init_test_db.ps1`、`docs/ops/deployment.md` §1.1／§3、`docs/testing/README.md` §3.1／§4、`docs/agents/tasks/W1-测试基建.md`、`docs/agents/README.md` §7.2、`docs/agents/工作计划.md`、`docs/README.md` | ① VM 内存 **1596 MB → 360 MB**（可用 1606 MB）；② Redis **Windows 侧协议实测 `PING` → `+PONG`**，`CONFIG GET maxmemory` = 268435456（256 MB）；③ `init_test_db.ps1` 输出 4 项全 PASS（DDL 改写 2 处、DROP+CREATE 成功、测试库表数 16、CHECK 约束已复制） | VM 磁盘仅剩 3.0 GB；VM 内**未做** Maven 构建验证（按铁律 6 不在低配机构建） |
| L1（本会话） | 主 Agent / 集成 + 验收 | **D7 JDK 21 安装与环境统一** | 环境变量（用户级 `JAVA_HOME`／`PATH`）、`scripts/toolchain-check/**`、`docs/ops/deployment.md` §3.1、`docs/agents/README.md` §7.2、`docs/agents/工作计划.md`、`docs/README.md` | ① 下载 Temurin 21 GA（195.6 MB / 44.7 秒，curl exit 0，ZIP 头校验通过）；② 安装到 `D:\develop\jdk21`（`java`/`javac`/`jcmd` 均 21.0.12.1）；③ **模拟全新 shell 验证**：`java -version` → 21.0.12.1，`mvn -v` → `Java version: 21.0.12.1, runtime: D:\develop\jdk21`；④ **端到端 Maven 构建**：`mvn clean package` exit 0（15.7 秒），jar 运行输出 `java.home = D:\develop\jdk21` 且 `RESULT: PASS`；⑤ 本地 Maven 仓库 606 MB 已落盘（中央仓库连通） | 机器级 PATH 仍留 `C:\jdk-25.0.2\bin`（**未动**，靠用户级 PATH 优先级压过）；`C:\Windows` 级改动一律未做 |
| L1（本会话） | 主 Agent / 集成 + 验收 | **W1 复核与 git 落地**：独立复核测试基建产出、裁决 CR-001、修覆盖率脚本缺陷、建 git 仓库 | `scripts/check_test_coverage_gaps.ps1`（修幽灵期望）、`docs/testing/验收项-测试映射.md`（新增「测试基建」节）、`.gitignore`、`.gitattributes` | ① 测试基建 10 个文件**逐个核对存在**、BOM 合规、**零越界**；② CI YAML 结构自检通过（281 行）；③ 覆盖率脚本**我亲自跑通**并修掉"幽灵期望"（期望 103→**102**，基建段 6/7→**6/6**）；④ `git init -b main`，112 文件入库，`target/`／`.tmp/` 经 `check-ignore` 确认被忽略 | Maven 层复跑**推迟到 M1 收工后**（避免抢 `~/.m2` 产生假失败）；当时 GitHub 凭据 401（账号密码已被 GitHub 弃用） |
| L1（本会话） | 主 Agent / 集成 + 验收 | **GitHub 远端落地** | `git remote` 配置、推送 `main` | ① 探测确认仓库存在（`git ls-remote`，注意 `ssh ... git-upload-pack` 在仓库存在时会挂死，是错误用法）；② `git push -u origin main` **exit 0**；③ **远端 112 文件 = 本地 112 文件**，逐个核对 CI 工作流、schema、脚本、pom、`.gitignore`、`.gitattributes` 均已送达；④ 上游 `origin/main` 已跟踪 | Private 仓库 + 无 PAT → **L1 无法查询 CI 运行结果**，需需求方在浏览器看 Actions |
| L1（本会话） | 主 Agent / 集成 + 验收 | **CR-002 处置预备**：新增契约导出脚本 | `scripts/export_openapi.ps1`（契约冻结的唯一入口） | 干跑验证：BOM 正确、5.1 语法 0 错误、**依赖探活有效**（MySQL/Redis 均探测为 True）、无 jar 时给出明确失败而非误判为契约问题 | **尚未真跑成功**（等 M1 的完整工程）；且发现 **M1 另写了 `server/export_openapi.ps1`** → **导出工具出现两份，需收敛为一份**（待 M1 收工后由 L1 裁决） |

| L1（本会话） | 主 Agent / 集成 + 验收 | **W1-M1 独立复核 + CR 裁决 + uni-app 变更** | `docs/adr/0011-前端改用uni-app作为唯一前端.md`（新增）、`docs/adr/0002`（转被取代）、`docs/adr/0003`（修订）、`AGENTS.md`（项目定位）、`docs/PLAN.md`（v1.15）、`docs/技术方案.md`（v1.8）、`docs/testing/README.md` §5.1、`docs/agents/工作计划.md` | ① **M1 复核（我自己的环境，不信它的输出）**：`mvn package` BUILD SUCCESS + `RequireJavaVersion passed`；`mvn test` **22/22 全绿**；`PACKAGE_EXIT=0 TEST_EXIT=0`；② 文件核对：`TempRateLimitDiagnosticTest` **已删**、重复导出脚本**已收敛**、`openapi.json` 合法（OpenAPI 3.0.1 / 8 路径 / SHA256 `F641BBF5…`）、**写权越界 0 项**、任务书点名测试 **15/15 存在**；③ 裁决 CR-002/003/004 并登记（`500` 已写入 §6.1 并附两条约束） | `openapi.json` 尚未由 L1 广播冻结（已在本轮广播）；CI 首次运行结果未能查询（Private + 无 PAT） |

### 5.3 交还所有权记录

| 路径 | 原所有者 | 交还时间 | 备注 |
|---|---|---|---|
| — | — | — | — |

---

## 6. 任务书索引

任务书由 L1 生成，存放于 `docs/agents/tasks/`（模板见 [`任务书模板.md`](任务书模板.md)）。

| 任务书 | 对应里程碑 | 派给 | 状态 |
|---|---|---|---|
| `tasks/W0-M0-数据库地基.md` | M0 | **L1 自己执行** | 不派发（M0 属契约与集成职责，见 [`README.md`](README.md) §1 的 L1 定位）；**执行待 MySQL 密码** |
| `tasks/W1-M1-后端骨架与认证.md` | M1 | L2 `20d4d1ce…ab130` | **已完成** —— 22 项测试全绿，L1 独立复核通过；遗留项见其交付报告 |
| `tasks/W1-测试基建.md` | — | 待派 L2（与上条**并行**） | **已创建（v2，已按 D6 决策改写为「不用 Testcontainers」）** —— 环境前置：仅 JDK 21（Redis 与测试库已就绪） |
| `tasks/W2-M3-帖子与OSS.md` | M3 | 待派 | 未创建 |
| `tasks/W2-M4-互动模块.md` | M4 | 待派 | 未创建 |
| `tasks/W2-M5-内容安全与通知.md` | M5 | 待派 | 未创建 |
| `tasks/W2-M2-前端骨架.md` | M2 | 待派 | 未创建（**依赖 `openapi.json` 冻结**，见 §4 广播） |

---

## 7. 变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.5 | 2026-09-14 | **W1 收尾**：M1 独立复核通过（22/22 全绿）；CR-002/003/004 裁决登记；**前端选型反转 uni-app** 并广播（含 M2 重写、M7 改打包）；广播 `openapi.json` 首个版本与 hash；新增两条实测坑（Surefire 不跑 `*IT.java`、`@Valid` 先于方法体） |
| v1.4 | 2026-09-14 | **GitHub 远端落地**：`main` 推送至 `git@github.com:tlt070606/hy-forum.git`（Private，**SSH 认证、无需 PAT**）；§1 版本控制行、§4 广播（含"首次 CI 预期为红"的说明）、§5.2 登记远端落地与 CR-002 预备。**遗留**：M1 另写了 `server/export_openapi.ps1` → 导出工具两份，待收敛 |
| v1.3 | 2026-09-14 | **D6 环境落地**：VM 腾出（他项目容器已 stop）、Redis 迁至 VM（`192.168.100.128:6380`，协议级实测通过）、新增测试库基建 `scripts/init_test_db.ps1`；§4 广播新增 D6 与 Agent 能力边界；§1／§5.2／§6 同步；W1 测试基建任务书改写为 v2（不用 Testcontainers） |
| v1.2 | 2026-09-14 | **W0 完成**：M0 在 MySQL 8.0.34 真机执行通过，`verify_m0.ps1` 升级为**双向** CHECK 验证（20 项全 PASS），并补上"编辑工具会丢 BOM"这一陷阱；§1／§5.2 登记验收输出。W1 两份任务书待派发，卡在环境前置 |
| v1.1 | 2026-09-14 | W0 契约冻结完成（P1-3／P1-4），§4 广播 3 条新决定；§2 所有权表改为具体包路径（依据 `技术方案.md` §3.3 v1.8 口径），CI 路径临时授予测试基建任务；§5 登记 L1 的 W0 产出与验收输出；§6 生成两份 W1 任务书 |
| v1.0 | 2026-09-14 | 建立协作看板：定义写权红线（仅 L1 写）、文件所有权、CR 队列、广播区、Agent 登记与任务书索引 |
