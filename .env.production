# ============================================================================
# 生产环境配置
# ----------------------------------------------------------------------------
# ⚠️ 当前**故意留空并给出明确失败**，而不是填一个假地址。
#
# 原因：项目定位是「本地可用版」（工作计划 §1 目标定位），域名尚未注册
# （PLAN.md A2 待办），备案未完成（A4 待办）。此时任何"看起来能用"的公网地址
# 都是编造的，会在上线前把人误导。
#
# 上线前必须由需求方回填（铁律 5：域名走环境变量，不写死在代码里）：
#   1. 填 https 域名（**必须 HTTPS**，否则小程序端与 Android 端都会被拒）
#   2. 该域名需已完成 ICP 备案（小程序端还要求域名已备案，见 ADR-0011 C2）
#
# 若未回填就执行 `npm run build:h5`，`src/utils/env.ts` 会在启动时抛出明确错误，
# 而不是静默地请求一个不存在的地址。这是有意的「快速失败」设计。
# ============================================================================

VITE_API_BASE_URL=

VITE_APP_ENV=production
