# ============================================================================
# 开发环境配置（uni-app + Vite）
# ----------------------------------------------------------------------------
# 为什么用 .env 而不是写死在代码里：铁律 5 —— 禁止硬编码域名与密钥。
# 前端产物（尤其 APK 与小程序包）可被反编译，任何写进代码的地址/密钥都等于公开。
#
# ⚠️ 这个值是**同源相对路径**，不是笔误，原因见下。
#
# --- 开发期统一走相对路径（H5 与小程序端一致）---
# 实测后端**没有任何 CORS 响应头**，且预检 OPTIONS 返回 403。
# H5 页面在 5173、后端在 8080，属跨源，浏览器会拦掉全部 /api 请求。
# 因此开发期统一使用相对路径：
#   - H5 端：由 vite.config.ts 的 server.proxy / preview.proxy 转发到本机后端
#   - 小程序端：uni.request 会把相对路径拼到**微信开发者工具的项目域名**上；
#     本地调试时配合「不校验合法域名」，指向 http://127.0.0.1:8080 即可
# 这样只需要维护**一个**基地址配置，而不是给 H5 与小程序各写一份。
#
# ⚠️ 关于「小程序端 request 只允许已备案 HTTPS 域名」（ADR-0011 C2）：
#    本地 http://127.0.0.1:8080 **在小程序正式环境不可用**。
#    本地调试靠微信开发者工具的「不校验合法域名」开关
#    （manifest.json 已配 mp-weixin.setting.urlCheck:false）。
#    这里不做任何"绕过"，因为那不是前端能解决的问题。
# ============================================================================

VITE_API_BASE_URL=/api

VITE_APP_ENV=development
