# ============================================================================
# 生产构建（mode=production：`uni build` 的默认模式，三端都走这里）
# ----------------------------------------------------------------------------
# ⚠️ `uni build` 只加载 `.env` 与 `.env.production`，**不读 `.env.development`**。
#    因此这里必须把"上线时后端在哪"说清楚。
# ============================================================================

# ---------------------------------------------------------------------------
# VITE_API_BASE_URL —— 唯一的上线开关
# ---------------------------------------------------------------------------
# 它在两个平台上的角色**完全不同**，所以必须一起说清楚：
#
# 【H5 端】本变量**不生效**。
#   H5 在 `src/utils/env.ts` 里被 `#ifdef H5` 强制为**同源相对根**，
#   上线时只需 Nginx 把 `/api` 反代到后端。
#   ⚠️ 这不是可选项：后端**没有任何 CORS 响应头，预检 OPTIONS 返回 403**（本机实测）。
#   H5 必须走 Nginx 同源反代，不能直连后端域名，否则浏览器会拦掉全部 /api 请求。
#
# 【小程序端 / App 端】本变量是**上线时唯一要改的地方**。
#   规则（实现见 `src/utils/env.ts` 的 getApiBaseUrl）：
#     · 留空           → 回退到 `.env` 的 `VITE_DEV_SERVER_ORIGIN`（本机后端）
#     · 填绝对地址     → 直接采用（必须 https，且域名已完成 ICP 备案）
#
# ⚠️ 今天留空意味着：`npm run build:mp-weixin` 打出的包会回退到本机 127.0.0.1 ——
#    **但它跑不起来**：production 模式下 `src/utils/env.ts`（第 167 行）会拒绝 http，
#    应用启动即抛错、一个请求都不发。**本地调试小程序端请改用**
#        npm run dev:mp-weixin      → dist/dev/mp-weixin
#    完整说明见 `小程序端人工验证.md` §0.1。
#    真机/正式环境连不上 —— 这属于本地开发状态，不是可交付状态。
#
# 上线步骤：
#   1. 取消下面一行的注释并填真实域名：
#        VITE_API_BASE_URL=https://api.你的域名.com
#   2. 重新构建：npm run build:mp-weixin / build:app
#
# 为什么现在不填一个假地址：域名尚未注册、备案未完成（后端仓库 PLAN.md A2/A4 待办）。
# 填假地址会让"看起来能上线"的错觉一直留到打包那天。
#
# ⚠️ 为什么不在这里写 VITE_DEV_SERVER_ORIGIN：
#    本文件一旦定义它就会**覆盖** `.env` 里的值，而那个值正是本地开发要用的。
#    因此本地/线上后端地址的切换统一由本变量表达，只留一个开关。
VITE_API_BASE_URL=

VITE_APP_ENV=production
