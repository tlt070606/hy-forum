# ============================================================================
# 开发模式专用（mode=development：dev:h5 / dev:mp-weixin / build:h5 的 dev 变体）
# ----------------------------------------------------------------------------
# ⚠️ 注意：`uni build` 走的是 **production 模式**，**不会读本文件**。
#    所以本文件里只放"只在开发模式才不同"的变量；
#    `VITE_DEV_SERVER_ORIGIN` 放在 `.env`（所有模式都加载），原因见该文件顶部。
# ============================================================================

# ---------------------------------------------------------------------------
# H5 端基地址（相对路径，走代理）
# ---------------------------------------------------------------------------
# H5 端保持**同源相对路径**，由 vite.config.ts 的 proxy（开发/E2E）
# 或 Nginx（生产）在服务端转发给后端。
#
# 为什么不给 H5 用绝对地址：后端**没有任何 CORS 响应头，预检 OPTIONS 返回 403**
# （本机实测）。H5 页面在 5173、后端在 8080 属跨源，浏览器会拦掉全部 /api 请求。
# 走代理后浏览器只发同源请求，问题从根上消失，且不需要后端任何配合。
#
# 注意：H5 端在 `src/utils/env.ts` 里被 `#ifdef H5` 强制为同源相对根（空字符串），
# 因此本变量对 H5 的基地址实际不生效，改它不会改变 H5 的请求地址。
# 它保留在这里是为了让"开发期后端在哪"这件事在配置里有明确落点。
VITE_API_BASE_URL=/api

VITE_APP_ENV=development
